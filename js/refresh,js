let to = new Date("23:59:0");
function update(){
    let from = new Date();
    diff=to-from;
    if(diff>0){
        sec=setTwoDigit(Math.floor(diff/1000)%60)
        document.querySelector(".timer").innerText=sec 
    }else {
        clearInterval(interval);
    }
}
